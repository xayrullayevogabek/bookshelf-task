export interface BookType {
  author: string;
  cover: string;
  id: number;
  isbn: string;
  pages: number;
  published: number;
  title: string;
}

export interface UserType {
  email: string;
  key: string;
  id: number;
  name: string;
  secret: string;
}
